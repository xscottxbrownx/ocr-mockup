import Box from "@mui/material/Box";

export default function CallToAction() {
  return (
    <Box
      sx={{
        minHeight: "20rem",
        width: "100vw",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <h2>CTA SECTION</h2>
    </Box>
  );
}
